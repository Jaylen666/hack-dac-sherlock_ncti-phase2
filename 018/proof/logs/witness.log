===== BUG-018 structural audit (single-tree, audited RTL only) =====

----- 1. the defect: two independent ifs assigning the same state -----
  PASS: hmac_core.sv:339 opens the init_cmd arm with a plain if
  PASS: hmac_core.sv:345 opens the next_cmd arm with a second plain if, not an else if
  PASS: hmac_core.sv:345 has no else, so both arms are evaluated in the same cycle
  PASS: the init_cmd arm assigns hmac_ctrl_new = CTRL_IPAD (hmac_core.sv:341)
  PASS: the next_cmd arm assigns the same variable hmac_ctrl_new = CTRL_OPAD (hmac_core.sv:348)
  PASS: both arms assert hmac_ctrl_we, so the last write wins and reaches the register

----- 2. in-file control: every other FSM state uses a single if -----
plain_if_total=5
else_if_total=0
ready_guarded_if=3
command_guarded_if=2
  PASS: census: exactly 2 command-guarded ifs in the FSM, both in CTRL_IDLE, both unchained
  PASS: census: the other 3 transition guards (IPAD/OPAD/HMAC_ready) are each a single if per state, so no other state can double-assign
  PASS: census: the FSM contains no else-if chain at all, so CTRL_IDLE's two arms are the only place the idiom could have applied
  PASS: the FSM itself is a unique case, so the file does use exclusivity constructs elsewhere

----- 3. both commands can genuinely be asserted in one cycle -----
  PASS: hmac.sv drives hmac_core's init_cmd from the register block
  PASS: hmac.sv drives hmac_core's next_cmd from the register block
  PASS: both commands originate in the software-written HMAC512_CTRL register, so one MMIO write can set both bits
  PASS: CTRL_IDLE reports ready, so such a write is accepted rather than rejected (hmac_core.sv:337)

----- 4. consequence: the IPAD phase is the keyed inner hash -----
  PASS: key_ipadded is the inner HMAC pad, computed from the key (hmac_core.sv:257)
  PASS: CTRL_IPAD is where H1 is launched on key_ipadded, so skipping it skips the inner hash
  PASS: CTRL_IPAD is also where set_entropy fires, so skipping it also skips the masking reseed

===== relevant source, quoted from the audited tree =====

--- src/hmac/rtl/hmac_core.sv:334-352 (the CTRL_IDLE command decode) ---

    unique case (hmac_ctrl_reg)
      CTRL_IDLE: begin
        ready_flag = 1'b1;

        if (init_cmd) begin
          digest_valid_new = 1'b0;
          hmac_ctrl_new    = CTRL_IPAD;
          hmac_ctrl_we     = 1'b1;
        end

        if (next_cmd) begin
          digest_valid_new = 1'b0;
          digest_valid_we  = 1'b1;
          hmac_ctrl_new    = CTRL_OPAD;
          hmac_ctrl_we     = 1'b1;
        end
      end


--- src/hmac/rtl/hmac_core.sv:353-372 (in-file controls: one guard per state) ---

        CTRL_IPAD:
          begin
            digest_valid_new = 0;
            digest_valid_we  = 1;
            if (IPAD_ready == 1)
              begin
                hmac_ctrl_new   = CTRL_OPAD;
                hmac_ctrl_we    = 1;
                set_entropy = 1;
              end
          end
        

        CTRL_OPAD:
          begin  
            digest_valid_new = 0;
            digest_valid_we  = 1;
            if (OPAD_ready == 1)
              begin

structural_gates_passed=17/17
RESULT: PASS
